# Implementation Plans

Generated by the improve skill on 2026-06-10, against commit `feed787`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

**Working-tree note**: at planning time the tree had uncommitted changes
(`src/index.ts`, `test/api.test.ts` modified; several agent-config files
staged). Plans were written against the **working tree**, not bare `feed787`.
Commit or stash deliberately before executing; the drift checks in each plan
will flag mismatches.

**Verification baseline at planning time**: `bunx tsc --noEmit` exit 0;
`bun run test` 24/24 passing in ~1.2s; `bun run check` failing with exactly 2
pre-existing errors (fixed by plan 001).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Verification baseline: typecheck script, green lint, CI | P1 | S | — | TODO |
| 002  | Backfill deadline-sweep and cancel-endpoint tests | P1 | S | 001 | TODO |
| 003  | Auth-gate the worker debug endpoints | P1 | S | 001 | TODO |
| 004  | Sweep stuck `queued` jobs past their deadline | P1 | S | 002 | TODO |
| 005  | Single active job per repo (partial unique index) + race-safe transitions | P2 | M | 002, 004 | TODO |
| 006  | Bound the run queries (SQL latest-run, list limits, cooldown) | P2 | M | 001 | TODO |
| 007  | Repo hygiene: gitignore, junk files, wrangler dedup, landing extraction | P3 | S | 001 | TODO |
| 008  | Input/auth hardening: timing-safe compare, bounds, owner/name validation | P3 | S | 001 (after 005, 006) | TODO |
| 009  | Backup visibility: latest/artifacts endpoints + restore design memo | P3 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always** — every other plan uses `bun run typecheck` / green `bun run check` as verification gates; neither exists/passes before 001.
- **002 before 004 and 005** — 004 and 005 modify `job-lifecycle.ts` / `scheduler.ts`; 002's characterization tests are their safety net.
- **004 before 005** — both edit `markFailedByDeadline`; 005's conditional-UPDATE rework assumes 004's queued-status acceptance is in place.
- **005, 006, 008 all touch `src/routes/repos.ts`** — execute serially in that order (005: trigger insert, 006: queries, 008: validation). Non-overlapping regions, but rebase between them.
- **007 and 009 both touch `src/index.ts` / `src/routes/repos.ts` lightly** — run after the above or expect trivial conflicts.
- **Operator action attached to 005**: remote D1 migration (`bunx wrangler d1 migrations apply gitcask-db --remote`) before the next production deploy.
- **Operator action attached to 007**: rotate `ADMIN_TOKEN` (value in untracked `.env` was exposed to local tooling transcripts during the audit).

## Findings considered and rejected

So nobody re-audits these:

- **Scheduler uses `Date.now()` instead of the tick-start timestamp for idempotency keys/deadlines** (`scheduler.ts:60,69`): drift is seconds-level with no behavioral consequence. Not worth doing.
- **PAT leakage via container error logs**: the one real vector (git clone stderr containing the embedded-PAT URL) is already sanitized at `container/server.ts:175`; other error paths (tar, S3, metadata fetch) don't carry the PAT. Not worth doing.
- **R2 cascade-delete needs backoff/batch caps** (`repos.ts:255-266`): cursor pagination is already correct; failure mode is speculative until someone deletes a repo with thousands of snapshots. Revisit only if observed.
- **Scheduler GitHub batching/caching/circuit breaker**: explicitly deferred in `review/ARCHITECTURE_DEEPENING.md` (Candidate 5) with re-open triggers; none have fired. Honor the deferral.
- **Container (`container/server.ts`) and CLI (`cli/`) unit tests**: real gap, but requires test infrastructure for a separate runtime (Bun server + S3 mocking) disproportionate to current risk. Revisit if container logic grows beyond clone/tar/upload.
- **`min_full_backup_days` is unused** (claimed by an audit subagent): **false** — it drives the periodic-full-backup floor at `src/services/change-detection.ts:29-33`. Recorded here so the wrong claim doesn't resurface.
- **`.env` is committed to git** (claimed by an audit subagent): **false** — it is untracked (verified via `git ls-files`). The true finding (not gitignored + live token) is handled in plan 007.
- **Per-repo retention overrides / change-detection opt-out** (direction): grounded but deferred by the maintainer's selection; retention is destructive-operation territory — propose again only with a concrete compliance need.
- **Staged agent-config files** (`.agents/`, `.codex/`, `.cursor/`, `.opencode/`, `.mcp.json`, `opencode.json`): intentionality unknown; deliberately left untouched by all plans. Operator decision.
