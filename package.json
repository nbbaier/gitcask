{
	"name": "gitcask",
	"module": "index.ts",

	"type": "module",
	"private": true,
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.13.2",
		"@types/bun": "latest",
		"drizzle-kit": "^0.31.10",
		"vitest": "^4.1.0",
		"wrangler": "^4.75.0"
	},
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"db:generate": "drizzle-kit generate",
		"db:migrate:local": "wrangler d1 migrations apply gitcask-db --local",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@cloudflare/workers-types": "^4.20260317.1",
		"drizzle-orm": "^0.45.1",
		"hono": "^4.12.8"
	}
}
